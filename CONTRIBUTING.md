# Contributing

Branch from captain/pulsev1c, push, ask Cy for review in this room.

Branch per task, named `<you>/<task>` (e.g. `dex/t1-pulse-schema`). See the
README for the `pulse.json` schema and the `GET /api/pulse` contract before
starting T1–T3.
